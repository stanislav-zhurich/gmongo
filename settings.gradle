new File("$settingsDir/components").eachDir { File comp ->
    include comp.name
    def thisSubProject = project(":$comp.name")
    thisSubProject.projectDir = comp
    thisSubProject.buildFileName = "${thisSubProject.name}.gradle"
	println comp.name
}
rootProject.name="gmongo"